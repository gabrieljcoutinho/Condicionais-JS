const compativel = 'laranja'

const fazerSuco = (compativel == 'laranja' || compativel == 'abacaxi') ? 'Fruta compatível para a receita.' : 'Fruta incompatível para a receita.'

console.log(fazerSuco)